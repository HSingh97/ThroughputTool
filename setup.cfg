[metadata]
# Name, version, author etc. are in pyproject.toml

[options]
packages = find:
package_dir =
    = .
[options.package_data]
# This tells setuptools to include the 'l2_flooder' executable
# from within the 'throughputtool' package directory.
throughputtool = l2_flooder